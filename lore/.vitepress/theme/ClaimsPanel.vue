<script setup lang="ts">import claims from '../../data/claims.json'</script><template><div v-for="c in claims.claims" :key="c.id" class="lore-card" :class="`status-${c.status}`"><strong>{{c.predicate}}</strong> <span class="lore-status">{{c.status}}</span><p><code>{{c.subject}}</code></p><pre>{{JSON.stringify(c.object,null,2)}}</pre><small>{{c.sources.join(', ')}}</small></div></template>
